{
    "name": "@libraries/types",
    "version": "0.0.1",
    "description": "Types ",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
      "build": "rm -rf dist && rm -f tsconfig.tsbuildinfo && tsc -p tsconfig.json",
      "build:watch": "tsc -p tsconfig.json -w"
    },
    "devDependencies": {
      "typescript": "^4.9.4"
    },
    "dependencies": {
      "zod": "^3.21.4"
    }
}
